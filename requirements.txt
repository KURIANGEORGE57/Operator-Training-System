# Core dependencies for Operator Training System
# Version pins ensure reproducible builds and prevent breaking changes

streamlit==1.51.0
pydantic==2.12.4
numpy==2.3.5
pandas==2.3.3

# Testing
pytest==9.0.1
